name = "Basanti"
age= 20
print("Your name & age are", name+age)

#Error: can only concatenate str (not "int") to str
# that means name and age variables are declared, but one is string and another is integer type of class.
# so only give output for string not with both together. so we get an error